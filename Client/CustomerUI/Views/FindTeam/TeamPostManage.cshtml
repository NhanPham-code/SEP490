<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bài Đăng Của Tôi - Nền Tảng Đặt Sân Thể Thao</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="~/css/FindTeam/TeamManage.css" asp-append-version="true" />
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Pacifico&display=swap" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/remixicon/4.2.0/remixicon.min.css" rel="stylesheet">
    
</head>
<body class="min-h-screen">
    <!-- Floating Background Elements -->
    <div class="floating-elements"></div>

    <!-- Main Content -->
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        <!-- Header Section -->
        <div class="mb-8">
            <div class="bg-white/80 backdrop-blur-sm rounded-3xl shadow-soft border border-white/20 p-8">
                <div class="flex flex-col lg:flex-row items-start lg:items-center justify-between gap-6">
                    <div class="flex-1">
                        <h1 class="text-3xl lg:text-4xl font-bold text-gray-900 mb-2">
                            Bài Đăng Thể Thao Của Tôi
                        </h1>
                        <p class="text-gray-600 text-lg">
                            Quản lý lời mời tham gia trận đấu và kết nối với các vận động viên khác
                        </p>
                        <div class="flex gap-4 mt-4">
                            <div class="stat-badge px-4 py-2 rounded-xl">
                                <span class="text-sm font-medium text-gray-700">2 Bài Đăng Hoạt Động</span>
                            </div>
                            <div class="stat-badge px-4 py-2 rounded-xl">
                                <span class="text-sm font-medium text-gray-700">12 Lượt Xem Tổng</span>
                            </div>
                        </div>
                    </div>
                    <div class="flex gap-3">
                        <a href="index.html" class="btn-primary text-white px-6 py-3 rounded-2xl text-sm font-semibold inline-flex items-center gap-2">
                            <i class="ri-add-line text-lg"></i>
                            Tạo Bài Đăng Mới
                        </a>
                    </div>
                </div>
            </div>
        </div>

        <!-- Posts Grid -->
        <div class="grid grid-cols-1 md:grid-cols-2 xl:grid-cols-3 gap-6">
            <!-- Post 1 -->
            <a asp-controller="TeamMember" asp-action="TeamManage" class="post-card bg-white/90 backdrop-blur-sm rounded-3xl shadow-soft border border-white/20 p-6 hover:shadow-large transition-all">
                <!-- Post Header -->
                <div class="flex items-start gap-4 mb-6">
                    <div class="avatar w-14 h-14 rounded-full flex items-center justify-center text-white font-bold text-lg">
                        <span>TV</span>
                    </div>
                    <div class="flex-1">
                        <h3 class="font-semibold text-gray-900 text-lg">Trần Văn Nam</h3>
                        <div class="flex items-center gap-2 mt-1">
                            <span class="bg-blue-100 text-blue-700 px-3 py-1 rounded-full text-xs font-medium">Trung Bình</span>
                            <span class="bg-green-100 text-green-700 px-3 py-1 rounded-full text-xs font-medium">5 Quan Tâm</span>
                        </div>
                    </div>
                </div>

                <!-- Sport Info -->
                <div class="bg-gradient-to-r from-orange-50 to-amber-50 rounded-2xl p-4 mb-6">
                    <div class="flex items-center gap-3 mb-3">
                        <div class="sport-icon">
                            <i class="ri-basketball-line text-lg"></i>
                        </div>
                        <span class="font-semibold text-gray-900">Trận Bóng Rổ</span>
                    </div>

                    <div class="space-y-3">
                        <div class="flex items-center text-sm text-gray-700">
                            <i class="ri-calendar-2-line mr-3 text-primary"></i>
                            <span class="font-medium">12 Tháng 8, 2025 lúc 18:00</span>
                        </div>
                        <div class="flex items-center text-sm text-gray-700">
                            <i class="ri-map-pin-2-line mr-3 text-secondary"></i>
                            <span class="font-medium">Sân Victory, Quận 1</span>
                        </div>
                        <div class="flex items-center text-sm text-gray-700">
                            <i class="ri-group-line mr-3 text-accent"></i>
                            <span class="font-medium">Tìm 3 người chơi</span>
                        </div>
                    </div>
                </div>

                <!-- Description -->
                <p class="text-gray-600 mb-6 leading-relaxed">
                    Tìm kiếm những người chơi nhiệt tình cho trận bóng rổ thân thiện. Chào mừng tất cả các trình độ! Hãy cùng vui chơi và rèn luyện sức khỏe.
                </p>

                <!-- Action Buttons -->
                <div class="flex gap-3">
                    <button class="btn-secondary text-white px-4 py-2.5 rounded-xl text-sm font-semibold flex-1 inline-flex items-center justify-center gap-2">
                        <i class="ri-edit-2-line"></i>
                        Chỉnh Sửa
                    </button>
                    <button class="btn-danger text-white px-4 py-2.5 rounded-xl text-sm font-semibold flex-1 inline-flex items-center justify-center gap-2 delete-post">
                        <i class="ri-delete-bin-line"></i>
                        Xóa
                    </button>
                </div>
            </a>

            <!-- Post 2 -->
            <div class="post-card bg-white/90 backdrop-blur-sm rounded-3xl shadow-soft border border-white/20 p-6 hover:shadow-large transition-all" data-url="post-detail.html">
                <!-- Post Header -->
                <div class="flex items-start gap-4 mb-6">
                    <div class="avatar w-14 h-14 rounded-full flex items-center justify-center text-white font-bold text-lg">
                        <span>LH</span>
                    </div>
                    <div class="flex-1">
                        <h3 class="font-semibold text-gray-900 text-lg">Lê Thị Hoa</h3>
                        <div class="flex items-center gap-2 mt-1">
                            <span class="bg-green-100 text-green-700 px-3 py-1 rounded-full text-xs font-medium">Mới Bắt Đầu</span>
                            <span class="bg-purple-100 text-purple-700 px-3 py-1 rounded-full text-xs font-medium">2 Quan Tâm</span>
                        </div>
                    </div>
                </div>

                <!-- Sport Info -->
                <div class="bg-gradient-to-r from-green-50 to-emerald-50 rounded-2xl p-4 mb-6">
                    <div class="flex items-center gap-3 mb-3">
                        <div class="sport-icon bg-gradient-to-r from-green-500 to-emerald-500">
                            <i class="ri-ping-pong-line text-lg"></i>
                        </div>
                        <span class="font-semibold text-gray-900">Luyện Tập Tennis</span>
                    </div>

                    <div class="space-y-3">
                        <div class="flex items-center text-sm text-gray-700">
                            <i class="ri-calendar-2-line mr-3 text-primary"></i>
                            <span class="font-medium">14 Tháng 8, 2025 lúc 10:00</span>
                        </div>
                        <div class="flex items-center text-sm text-gray-700">
                            <i class="ri-map-pin-2-line mr-3 text-secondary"></i>
                            <span class="font-medium">Sân Grand Slam</span>
                        </div>
                        <div class="flex items-center text-sm text-gray-700">
                            <i class="ri-user-line mr-3 text-accent"></i>
                            <span class="font-medium">Tìm 1 đối tác</span>
                        </div>
                    </div>
                </div>

                <!-- Description -->
                <p class="text-gray-600 mb-6 leading-relaxed">
                    Tìm kiếm đối tác tennis để luyện tập. Hoàn hảo cho người mới bắt đầu muốn cải thiện kỹ năng trong môi trường thân thiện.
                </p>

                <!-- Action Buttons -->
                <div class="flex gap-3">
                    <button class="btn-secondary text-white px-4 py-2.5 rounded-xl text-sm font-semibold flex-1 inline-flex items-center justify-center gap-2">
                        <i class="ri-edit-2-line"></i>
                        Chỉnh Sửa
                    </button>
                    <button class="btn-danger text-white px-4 py-2.5 rounded-xl text-sm font-semibold flex-1 inline-flex items-center justify-center gap-2 delete-post">
                        <i class="ri-delete-bin-line"></i>
                        Xóa
                    </button>
                </div>
            </div>

            <!-- Empty State Card -->
            <div class="post-card empty-state border-2 border-dashed border-gray-200 rounded-3xl p-8 flex flex-col items-center justify-center text-center min-h-[400px]">
                <div class="w-20 h-20 bg-gradient-to-br from-gray-100 to-gray-200 rounded-full flex items-center justify-center mb-4">
                    <i class="ri-add-line text-3xl text-gray-400"></i>
                </div>
                <h3 class="font-semibold text-gray-700 mb-2">Tạo Bài Đăng Tiếp Theo</h3>
                <p class="text-gray-500 text-sm mb-4">Chia sẻ hoạt động thể thao và kết nối với những người chơi cùng sở thích</p>
                <a href="index.html" class="btn-primary text-white px-6 py-3 rounded-xl text-sm font-semibold inline-flex items-center gap-2">
                    <i class="ri-add-line"></i>
                    Thêm Bài Đăng Mới
                </a>
            </div>
        </div>

        <!-- Quick Stats Section -->
        <div class="mt-12">
            <div class="bg-white/80 backdrop-blur-sm rounded-3xl shadow-soft border border-white/20 p-8">
                <h2 class="text-2xl font-bold text-gray-900 mb-6">Tổng Quan Hoạt Động Của Bạn</h2>
                <div class="grid grid-cols-1 md:grid-cols-4 gap-6">
                    <div class="text-center p-4">
                        <div class="w-16 h-16 bg-gradient-to-br from-blue-500 to-blue-600 rounded-full flex items-center justify-center mx-auto mb-3">
                            <i class="ri-file-list-3-line text-2xl text-white"></i>
                        </div>
                        <div class="text-2xl font-bold text-gray-900">2</div>
                        <div class="text-sm text-gray-600">Bài Đăng Hoạt Động</div>
                    </div>
                    <div class="text-center p-4">
                        <div class="w-16 h-16 bg-gradient-to-br from-green-500 to-green-600 rounded-full flex items-center justify-center mx-auto mb-3">
                            <i class="ri-eye-line text-2xl text-white"></i>
                        </div>
                        <div class="text-2xl font-bold text-gray-900">47</div>
                        <div class="text-sm text-gray-600">Tổng Lượt Xem</div>
                    </div>
                    <div class="text-center p-4">
                        <div class="w-16 h-16 bg-gradient-to-br from-purple-500 to-purple-600 rounded-full flex items-center justify-center mx-auto mb-3">
                            <i class="ri-heart-line text-2xl text-white"></i>
                        </div>
                        <div class="text-2xl font-bold text-gray-900">7</div>
                        <div class="text-sm text-gray-600">Người Quan Tâm</div>
                    </div>
                    <div class="text-center p-4">
                        <div class="w-16 h-16 bg-gradient-to-br from-orange-500 to-orange-600 rounded-full flex items-center justify-center mx-auto mb-3">
                            <i class="ri-trophy-line text-2xl text-white"></i>
                        </div>
                        <div class="text-2xl font-bold text-gray-900">5</div>
                        <div class="text-sm text-gray-600">Trận Đã Hoàn Thành</div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- JavaScript -->
    <script>
        // Enhanced delete confirmation with modern modal
        document.querySelectorAll('.delete-post').forEach(button => {
            button.addEventListener('click', (e) => {
                e.stopPropagation();

                // Create custom modal
                const modal = document.createElement('div');
                modal.className = 'modal fixed inset-0 bg-black/50 backdrop-blur-sm flex items-center justify-center z-50 p-4';
                modal.innerHTML = `
                    <div class="modal-content bg-white rounded-3xl p-8 max-w-md w-full shadow-2xl transform transition-all">
                        <div class="text-center">
                            <div class="w-16 h-16 bg-red-100 rounded-full flex items-center justify-center mx-auto mb-4">
                                <i class="ri-delete-bin-line text-2xl text-red-600"></i>
                            </div>
                            <h3 class="text-xl font-bold text-gray-900 mb-2">Xóa Bài Đăng</h3>
                            <p class="text-gray-600 mb-6">Bạn có chắc chắn muốn xóa bài đăng này? Hành động này không thể hoàn tác.</p>
                            <div class="flex gap-3">
                                <button class="cancel-btn flex-1 bg-gray-100 text-gray-700 px-6 py-3 rounded-xl font-semibold hover:bg-gray-200 transition-colors">
                                    Hủy
                                </button>
                                <button class="confirm-btn flex-1 bg-gradient-to-r from-red-500 to-red-600 text-white px-6 py-3 rounded-xl font-semibold hover:from-red-600 hover:to-red-700 transition-all">
                                    Xóa
                                </button>
                            </div>
                        </div>
                    </div>
                `;

                document.body.appendChild(modal);

                // Handle modal actions
                modal.querySelector('.cancel-btn').addEventListener('click', () => {
                    modal.remove();
                });

                modal.querySelector('.confirm-btn').addEventListener('click', () => {
                    const postCard = button.closest('.post-card');
                    postCard.style.transition = 'all 0.5s ease';
                    postCard.style.transform = 'scale(0.8) rotate(5deg)';
                    postCard.style.opacity = '0';

                    setTimeout(() => {
                        postCard.remove();
                    }, 500);

                    modal.remove();
                });

                modal.addEventListener('click', (e) => {
                    if (e.target === modal) {
                        modal.remove();
                    }
                });
            });
        });

        // Enhanced click to open post detail
        document.querySelectorAll('.post-card[data-url]').forEach(card => {
            card.addEventListener('click', (e) => {
                if (!e.target.closest('button')) {
                    const url = card.getAttribute('data-url');

                    // Add loading state
                    card.classList.add('loading');

                    setTimeout(() => {
                        window.location.href = url;
                    }, 500);
                }
            });
        });

        // Add smooth scroll behavior for better UX
        document.documentElement.style.scrollBehavior = 'smooth';

        // Add entrance animations
        const observerOptions = {
            threshold: 0.1,
            rootMargin: '0px 0px -50px 0px'
        };

        const observer = new IntersectionObserver((entries) => {
            entries.forEach((entry, index) => {
                if (entry.isIntersecting) {
                    setTimeout(() => {
                        entry.target.style.opacity = '1';
                        entry.target.style.transform = 'translateY(0)';
                    }, index * 100);
                }
            });
        }, observerOptions);

        // Initialize animations
        document.querySelectorAll('.post-card').forEach((card, index) => {
            card.style.opacity = '0';
            card.style.transform = 'translateY(20px)';
            card.style.transition = 'all 0.6s cubic-bezier(0.4, 0, 0.2, 1)';
            observer.observe(card);
        });
    </script>
</body>
</html>