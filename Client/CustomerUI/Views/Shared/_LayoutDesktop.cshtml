@{
    var userId = Context.Session.GetInt32("UserId");
}
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title translate="no">@ViewData["Title"] - Sportivey</title>

    <link rel="icon" type="image/png" href="/favicon.png" />
    <meta name="description" content="Book your favorite sports stadiums with ease and flexibility." />

    <!-- Styles -->
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Inter:wght@100;200;300;400;500;600;700;800;900&family=Poppins:wght@100;200;300;400;500;600;700;800;900&display=swap"
        rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/remixicon/4.6.0/remixicon.min.css" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com/3.4.16"></script>
    <script src="https://unpkg.com/alpinejs@3.x.x/dist/cdn.min.js" defer></script>
    <script src="https://code.jquery.com/jquery-3.7.0.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/microsoft-signalr/7.0.5/signalr.min.js"></script>
    <style>
        /* Hide Google Translate banner and unwanted elements */
        .goog-te-banner-frame {
            display: none !important;
        }

        iframe.goog-te-banner-frame {
            display: none !important;
        }

        body {
            top: 0px !important;
            margin: 0 !important;
            position: static !important;
        }

        .goog-logo-link {
            display: none !important;
        }

        .goog-te-gadget {
            color: transparent !important;
            font-size: 0 !important;
        }

            .goog-te-gadget .goog-te-combo {
                margin: 0 !important;
            }

        /* Style the translate dropdown to match navbar */
       .goog-te-combo {
    appearance: none !important; /* bỏ style mặc định */
    background: transparent !important;
    border: none !important;
    color: #475569 !important; /* text-slate-600 */
    font-weight: 600 !important;
    padding: 0 !important;
    font-size: 14px !important;
    cursor: pointer !important;
}
.goog-te-combo:hover {
    color: #2563eb !important; /* hover:text-blue-600 */
}


            .goog-te-combo option {
                background: white !important;
                color: #475569 !important;
                font-weight: 600 !important;
            }

        /* Hide unwanted translate elements */
        .skiptranslate iframe,
        iframe[src*="translate.googleapis.com"] {
            visibility: hidden !important;
            display: none !important;
        }

        /* Custom translate container styling */
        .translate-container {
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .translate-icon {
            color: #475569;
            font-size: 16px;
        }

        /* Body styling for demo */
        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            margin: 0;
            padding: 20px;
        }

        .nav-link.active {
            background: rgba(59, 130, 246, 0.1);
            border-radius: 8px;
            color: #2563eb !important;
        }

        .nav-link:hover:not(.active) {
            background: rgba(255, 255, 255, 0.1);
            border-radius: 8px;
        }

        /* Demo content styling */
        .demo-content {
            background: rgba(255,255,255,0.15);
            padding: 25px;
            border-radius: 15px;
            margin: 25px auto;
            max-width: 800px;
            backdrop-filter: blur(15px);
            border: 1px solid rgba(255,255,255,0.2);
            box-shadow: 0 8px 32px rgba(0,0,0,0.1);
            color: white;
        }

        .goog-tooltip,
        .goog-tooltip:hover,
        .goog-text-highlight,
        .goog-te-balloon-frame,
        #goog-gt-tt {
            display: none !important;
            visibility: hidden !important;
        }

        #goog-gt-tt,
        .goog-te-balloon-frame,
        .goog-te-banner-frame,
        .goog-tooltip,
        .goog-text-highlight {
            display: none !important;
        }

    </style>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        'inter': ['Inter', 'sans-serif'],
                        'poppins': ['Poppins', 'sans-serif'],
                    },
                    colors: {
                        primary: {
                            50: '#eff6ff',
                            100: '#dbeafe',
                            200: '#bfdbfe',
                            300: '#93c5fd',
                            400: '#60a5fa',
                            500: '#3b82f6',
                            600: '#2563eb',
                            700: '#1d4ed8',
                            800: '#1e40af',
                            900: '#1e3a8a',
                            DEFAULT: '#3b82f6',
                        },
                        secondary: {
                            50: '#f0fdf4',
                            100: '#dcfce7',
                            200: '#bbf7d0',
                            300: '#86efac',
                            400: '#4ade80',
                            500: '#22c55e',
                            600: '#16a34a',
                            700: '#15803d',
                            800: '#166534',
                            900: '#14532d',
                            DEFAULT: '#22c55e',
                        },
                        accent: {
                            50: '#faf5ff',
                            100: '#f3e8ff',
                            200: '#e9d5ff',
                            300: '#d8b4fe',
                            400: '#c084fc',
                            500: '#a855f7',
                            600: '#9333ea',
                            700: '#7c2d12',
                            800: '#6b21a8',
                            900: '#581c87',
                            DEFAULT: '#a855f7',
                        }
                    },
                    animation: {

                        'slide-up': 'slideUp 0.5s ease-out forwards',
                        'slide-down': 'slideDown 0.3s ease-out forwards',
                        'bounce-subtle': 'bounceSubtle 2s infinite',
                        'glow': 'glow 2s ease-in-out infinite alternate',
                        'float': 'float 6s ease-in-out infinite',
                        'gradient-shift': 'gradientShift 8s ease-in-out infinite',
                        'pulse-slow': 'pulse 3s cubic-bezier(0.4, 0, 0.6, 1) infinite',
                        'expand': 'expand 0.3s ease-out forwards',
                        'collapse': 'collapse 0.3s ease-out forwards',
                        'shake': 'shake 0.5s ease-in-out',
                    },
                    backdropBlur: {
                        xs: '2px',
                        sm: '4px',
                        DEFAULT: '8px',
                        md: '12px',
                        lg: '16px',
                        xl: '24px',
                        '2xl': '40px',
                        '3xl': '64px',
                    },
                },
            },
        };

        // Enhanced animations
        const style = document.createElement('style');
        style.textContent = `
                @@keyframes fadeIn {
                    from { opacity: 0; transform: translateY(-20px); }
                    to { opacity: 1; transform: translateY(0); }
                }
                @@keyframes slideUp {
                    from { opacity: 0; transform: translateY(30px); }
                    to { opacity: 1; transform: translateY(0); }
                }
                @@keyframes slideDown {
                    from { opacity: 0; transform: translateY(-20px); }
                    to { opacity: 1; transform: translateY(0); }
                }
                @@keyframes bounceSubtle {
                    0%, 100% { transform: translateY(0px); }
                    50% { transform: translateY(-4px); }
                }
                @@keyframes glow {
                    0% { box-shadow: 0 0 5px rgba(59, 130, 246, 0.5); }
                    100% { box-shadow: 0 0 20px rgba(59, 130, 246, 0.8); }
                }
                @@keyframes float {
                    0%, 100% { transform: translateY(0px); }
                    50% { transform: translateY(-6px); }
                }
                @@keyframes gradientShift {
                    0% { background-position: 0% 50%; }
                    50% { background-position: 100% 50%; }
                    100% { background-position: 0% 50%; }
                }
                @@keyframes expand {
                    from { width: 0; opacity: 0; }
                    to { width: 320px; opacity: 1; }
                }
                @@keyframes collapse {
                    from { width: 320px; opacity: 1; }
                    to { width: 0; opacity: 0; }
                }
                @@keyframes shake {
                    0%, 100% { transform: translateX(0); }
                    25% { transform: translateX(-3px); }
                    75% { transform: translateX(3px); }
                }
                @@keyframes slideInRight {
                    from { opacity: 0; transform: translateX(20px); }
                    to { opacity: 1; transform: translateX(0); }
                }
                @@keyframes slideOutRight {
                    from { opacity: 1; transform: translateX(0); }
                    to { opacity: 0; transform: translateX(20px); }
                }
                `;
        document.head.appendChild(style);
    </script>

    <!-- Section for custom styles from view -->
    @RenderSection("Styles", required: false)
    <style>
        /* Enhanced Global Styles & Animations */
        * {
            font-family: 'Inter', sans-serif;
            box-sizing: border-box;
        }

        body {
            -webkit-font-smoothing: antialiased;
            -moz-osx-font-smoothing: grayscale;
            background: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 100%);
            position: relative;
            overflow-x: hidden;
            margin: 0;
            padding: 0;
        }

        #nav{
            display: flex;
        }

        ::-webkit-scrollbar {
            width: 6px;
        }

        ::-webkit-scrollbar-track {
            background: #f1f5f9;
        }

        ::-webkit-scrollbar-thumb {
            background: linear-gradient(135deg, #3b82f6, #8b5cf6);
            border-radius: 3px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: linear-gradient(135deg, #2563eb, #7c3aed);
        }

        .header-glass {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(20px) saturate(180%);
            border-bottom: 1px solid rgba(255, 255, 255, 0.2);
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.08);
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }

        .logo-gradient {
            background: linear-gradient(135deg, #3b82f6, #8b5cf6, #22c55e);
            background-size: 300% 300%;
            animation: gradientShift 8s ease-in-out infinite;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .btn-gradient {
            background: linear-gradient(135deg, #3b82f6, #8b5cf6);
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            position: relative;
            overflow: hidden;
            box-shadow: 0 4px 15px rgba(59, 130, 246, 0.3);
        }

        .btn-gradient:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 30px rgba(59, 130, 246, 0.4);
        }

        .btn-gradient:active {
            transform: translateY(0);
        }

        .glass-effect {
            background: rgba(255, 255, 255, 0.85);
            backdrop-filter: blur(20px) saturate(120%);
            border: 1px solid rgba(255, 255, 255, 0.3);
            transition: all 0.3s ease;
        }

        .glass-effect:hover {
            background: rgba(255, 255, 255, 0.9);
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
        }

        .search-container {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(12px);
            border: 2px solid rgba(255, 255, 255, 0.4);
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
        }

        .search-container:focus-within {
            background: rgba(255, 255, 255, 1);
            border-color: rgba(59, 130, 246, 0.5);
            box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.1), 0 4px 25px rgba(0, 0, 0, 0.12);
            transform: translateY(-1px);
        }

        .nav-link {
            position: relative;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            border-radius: 16px;
            overflow: hidden;
        }

        .nav-link:hover {
            color: #3b82f6;
            background: rgba(59, 130, 246, 0.08);
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(59, 130, 246, 0.15);
        }

        .nav-link.active {
            color: #3b82f6;
            background: rgba(59, 130, 246, 0.12);
            box-shadow: 0 4px 15px rgba(59, 130, 246, 0.2);
        }

        .nav-link::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 0;
            height: 100%;
            background: linear-gradient(135deg, rgba(59, 130, 246, 0.1), rgba(139, 92, 246, 0.1));
            transition: width 0.3s ease;
            z-index: -1;
        }

        .nav-link:hover::before {
            width: 100%;
        }

        .action-btn {
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            position: relative;
            overflow: visible;
        }

        .action-btn:hover {
            transform: translateY(-3px) scale(1.05);
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
        }

        .action-btn:active {
            transform: translateY(-1px) scale(0.98);
        }

        .notification-badge {
            animation: pulse-slow 3s cubic-bezier(0.4, 0, 0.6, 1) infinite;
            z-index: 10;
        }

        .notification-dot {
            animation: shake 0.8s ease-in-out infinite;
            z-index: 10;
        }

        .badge-container {
            position: relative;
            display: inline-block;
        }

        .badge-container .notification-badge {
            position: absolute;
            top: -6px;
            right: -6px;
            z-index: 15;
        }

        .badge-container .notification-dot {
            position: absolute;
            top: -2px;
            right: -2px;
            z-index: 15;
        }

        .dropdown-solid {
            background: #ffffff !important;
            backdrop-filter: none !important;
            border: 1px solid #e2e8f0;
            box-shadow: 0 25px 50px rgba(0, 0, 0, 0.15);
        }

        @@media (max-width: 768px) {
            .header-glass {
                padding: 0 16px;
            }

            .nav-section {
                display: none;
            }

            .mobile-nav-toggle {
                display: block;
            }
        }

        .tooltip {
            position: absolute;
            z-index: 1000;
            padding: 8px 12px;
            background: rgba(15, 23, 42, 0.9);
            backdrop-filter: blur(8px);
            color: white;
            font-size: 0.75rem;
            font-weight: 500;
            border-radius: 8px;
            white-space: nowrap;
            pointer-events: none;
            opacity: 0;
            transform: translateY(4px);
            transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
        }

        .tooltip.show {
            opacity: 1;
            transform: translateY(0);
        }

        .notification-panel,
        .chat-panel,
        .search-suggestions {
            background: rgba(255, 255, 255, 0.98);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(226, 232, 240, 0.8);
            box-shadow: 0 25px 50px rgba(0, 0, 0, 0.15);
        }

        .logo-container:hover .animate-float {
            animation-duration: 2s;
        }

        .search-suggestions {
            border: 1px solid rgba(226, 232, 240, 0.5);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
        }

        /* Animations */
        @@keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(-20px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @@keyframes slideUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @@keyframes slideDown {
            from {
                opacity: 0;
                transform: translateY(-20px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @@keyframes bounceSubtle {

            0%,
            100% {
                transform: translateY(0px);
            }

            50% {
                transform: translateY(-4px);
            }
        }

        @@keyframes glow {
            0% {
                box-shadow: 0 0 5px rgba(59, 130, 246, 0.5);
            }

            100% {
                box-shadow: 0 0 20px rgba(59, 130, 246, 0.8);
            }
        }

        @@keyframes float {

            0%,
            100% {
                transform: translateY(0px);
            }

            50% {
                transform: translateY(-6px);
            }
        }

        @@keyframes gradientShift {
            0% {
                background-position: 0% 50%;
            }

            50% {
                background-position: 100% 50%;
            }

            100% {
                background-position: 0% 50%;
            }
        }

        @@keyframes pulse-slow {
            0% {
                box-shadow: 0 0 0 0 #3b82f6;
            }

            70% {
                box-shadow: 0 0 0 10px rgba(59, 130, 246, 0);
            }

            100% {
                box-shadow: 0 0 0 0 rgba(59, 130, 246, 0);
            }
        }

        @@keyframes expand {
            from {
                width: 0;
                opacity: 0;
            }

            to {
                width: 320px;
                opacity: 1;
            }
        }

        @@keyframes collapse {
            from {
                width: 320px;
                opacity: 1;
            }

            to {
                width: 0;
                opacity: 0;
            }
        }

        @@keyframes shake {

            0%,
            100% {
                transform: translateX(0);
            }

            25% {
                transform: translateX(-3px);
            }

            75% {
                transform: translateX(3px);
            }
        }

        @@keyframes slideInRight {
            from {
                opacity: 0;
                transform: translateX(20px);
            }

            to {
                opacity: 1;
                transform: translateX(0);
            }
        }

        @@keyframes slideOutRight {
            from {
                opacity: 1;
                transform: translateX(0);
            }

            to {
                opacity: 0;
                transform: translateX(20px);
            }
        }
    </style>
</head>

<body class="bg-gradient-to-br from-slate-50 via-blue-50 to-indigo-50 text-slate-800 font-inter">
    <!-- Enhanced Header with improved layout -->
    <header x-data="{
            mobileMenuOpen: false,
            userMenuOpen: false,
            searchExpanded: false,
            notificationOpen: false,
            chatOpen: false,
            searchQuery: '' ,
            toggleSearch() {
            this.searchExpanded=!this.searchExpanded;
            if (this.searchExpanded) {
            this.$nextTick(()=>
        this.$refs.searchInput?.focus());
        } else {
        this.searchQuery = '';
        }
        },
        toggleNotifications() {
        this.notificationOpen = !this.notificationOpen;
        this.chatOpen = false;
        },
        toggleChat() {
        window.location.href = '@Url.Action("Chat", "Chat")';
        }


        }" class="header-glass sticky top-0 z-50 transition-all duration-300" id="header-pc">
        <div class="max-w-8xl mx-auto px-4 lg:px-8" id="header-mobile">
            <div class="flex items-center justify-between h-18">

                <!-- Logo Section - Improved proportions -->
                <div class="flex items-center flex-shrink-0 min-w-fit logo-container">
                    <a asp-controller="Home" asp-action="Index"
                        class="flex items-center space-x-3 transition-all duration-300 hover:scale-105 group">
                        <div
                            class="w-11 h-11 bg-gradient-to-br from-blue-500 via-purple-500 to-cyan-500 rounded-2xl flex items-center justify-center shadow-lg group-hover:shadow-xl transition-all duration-300 group-hover:rotate-12">
                            <i class="ri-football-line text-white text-xl animate-float"></i>
                        </div>
                        <h1 class="font-poppins font-bold text-xl logo-gradient hidden sm:block" translate="no">
                            Sportivey
                        </h1>
                    </a>
                </div>

                <!-- Desktop Navigation - Enhanced spacing and visual hierarchy -->
                <nav class="hidden lg:flex items-center justify-center flex-1 px-8 nav-section" >
                    <div id="nav"
                        class="flex items-center space-x-1 bg-white/60 backdrop-blur-sm rounded-2xl p-2 border border-white/40">
                        <a asp-controller="Home" asp-action="Index"
                            class="nav-link active px-4 py-2.5 text-slate-600 hover:text-blue-600 font-semibold flex items-center space-x-2 text-sm">
                            <i class="ri-home-4-line text-base"></i>
                            <span>Home</span>
                        </a>

                        <a asp-controller="FindTeam" asp-action="FindTeam"
                            class="nav-link px-4 py-2.5 text-slate-600 hover:text-blue-600 font-semibold flex items-center space-x-2 text-sm">
                            <i class="ri-team-line text-base"></i>
                            <span>Teams</span>
                        </a>


                        <a href="#"
                            class="nav-link px-4 py-2.5 text-slate-600 hover:text-blue-600 font-semibold flex items-center space-x-2 text-sm">

                            <i class="ri-information-line text-base"></i>
                            <span>About</span>
                        </a>

                        <!-- Thêm Google Translate vào đây -->
                        <div id="google_translate_element" class="hidden"></div>

                        <!-- Dropdown Translate -->
                        <div class="relative ml-4">
                            <!-- Nút chính -->
                            <button id="translate-toggle"
                                    type="button"
                                    class="nav-link px-4 py-2.5 text-slate-600 hover:text-blue-600 font-semibold flex items-center space-x-2 text-sm">
                                <i class="ri-translate-2 text-base"></i>
                                <span>Translate</span>
                                <i class="ri-arrow-down-s-line text-base"></i>
                            </button>

                            <!-- Menu xổ xuống -->
                            <div id="translate-menu"
                                 class="absolute right-0 mt-2 w-40 bg-white rounded-lg shadow-lg border border-gray-200 hidden z-50">
                                <button data-lang="en"
                                        class="block w-full text-left px-4 py-2 text-slate-600 hover:text-blue-600 text-sm">
                                    English
                                </button>
                                <button data-lang="vi"
                                        class="block w-full text-left px-4 py-2 text-slate-600 hover:text-blue-600 text-sm">
                                    Tiếng Việt
                                </button>
                                <button data-lang="ja"
                                        class="block w-full text-left px-4 py-2 text-slate-600 hover:text-blue-600 text-sm">
                                    日本語
                                </button>
                                <button data-lang="ko"
                                        class="block w-full text-left px-4 py-2 text-slate-600 hover:text-blue-600 text-sm">
                                    한국어
                                </button>
                                <button data-lang="zh-CN"
                                        class="block w-full text-left px-4 py-2 text-slate-600 hover:text-blue-600 text-sm">
                                    中文
                                </button>
                            </div>
                        </div>
                        <button id="reset-translate-btn"
                                type="button"
                                class="px-4 py-2.5 text-slate-600 hover:text-blue-600 font-semibold flex items-center space-x-2 text-sm rounded-xl transition-all duration-200 ml-3"
                                aria-pressed="false"
                                title="Đặt về mặc định">
                            <i class="ri-refresh-line text-base"></i>
                            <span>Đặt về mặc định</span>
                        </button>


                    </div>
                </nav>
                <!-- Right Section - Enhanced with better spacing and organization -->
                <div class="flex items-center space-x-3 min-w-fit">

                    @{
                        var accessToken = Context.Request.Cookies["AccessToken"];
                        Console.WriteLine("AccessToken: " + accessToken); // Debugging line to check token value
                        var fullName = Context.Session.GetString("FullName");
                        Console.WriteLine("FullName: " + fullName); // Debugging line to check full name value
                        var avatarUrl = Context.Session.GetString("AvatarUrl");
                    }

                    @if (!string.IsNullOrEmpty(accessToken) && !string.IsNullOrEmpty(fullName))
                    {
                        <!-- Enhanced User Actions Section -->
                        <div class="flex items-center space-x-3">
                            <!-- Enhanced Notifications with proper spacing -->
                            <div class="relative badge-container">
                                <a asp-controller="Notification" asp-action="Index"
                                    class="action-btn p-3 bg-gradient-to-br from-orange-500 to-red-500 rounded-xl text-white shadow-lg hover:shadow-xl group"
                                    title="Notifications">
                                    <i
                                        class="ri-notification-3-line text-lg group-hover:scale-110 transition-transform duration-200"></i>
                                    <!-- Badge số lượng thông báo chưa đọc -->
                                    <span id="notification-badge"
                                        class="notification-badge absolute -top-2 -right-2 w-5 h-5 bg-red-500 rounded-full flex items-center justify-center text-xs text-white font-bold z-10"
                                        style="display:none"></span>
                                </a>
                            </div>

                            <!-- Enhanced Chat/Messages with proper spacing -->
                            <div x-data="chatBadge()" x-init="init()" class="relative badge-container">
                                <a href="javascript:void(0);"
                                   class="action-btn p-3 bg-gradient-to-br from-purple-500 to-indigo-500 rounded-xl text-white shadow-lg hover:shadow-xl group"
                                   title="Messages"
                                   @@click="openChat()">
                                    <i class="ri-message-3-line text-lg group-hover:scale-110 transition-transform duration-200"></i>
                                    <template x-if="unreadCount > 0">
                                        <span class="notification-badge absolute -top-2 -right-2 w-5 h-5 bg-green-500 rounded-full flex items-center justify-center text-xs text-white font-bold z-10"
                                              x-text="unreadCount"></span>
                                    </template>
                                </a>
                            </div>
                            
                            <!-- Enhanced User Menu -->
                            <div class="relative" x-data="{ open: false }">
                                <button @@click="open = !open"
                                    class="flex items-center space-x-2 p-2 rounded-xl glass-effect hover:bg-white/80 transition-all duration-300 group">
                                    <img src="@avatarUrl" alt="Avatar"
                                        class="w-8 h-8 rounded-lg border-2 border-white/60 shadow-sm group-hover:scale-105 transition-transform duration-300"
                                        data-user-avatar />
                                    <div class="hidden xl:block text-left" translate="no">
                                        <p class="text-sm font-bold text-slate-800 leading-none" data-user-firstname>
                                            @(fullName?.Split(' ').FirstOrDefault() ?? "User")</p>
                                        <p class="text-xs text-slate-600 font-medium">Premium</p>
                                    </div>
                                    <i class="ri-arrow-down-s-line text-slate-500 group-hover:text-blue-600 transition-all duration-300 text-sm"
                                        :class="{ 'rotate-180': open }"></i>
                                </button>

                                <!-- Enhanced User Dropdown -->
                                <div x-show="open" x-transition:enter="transition ease-out duration-200"
                                    x-transition:enter-start="opacity-0 scale-95 translate-y-2"
                                    x-transition:enter-end="opacity-1 scale-100 translate-y-0"
                                    x-transition:leave="transition ease-in duration-150"
                                    x-transition:leave-start="opacity-1 scale-100 translate-y-0"
                                    x-transition:leave-end="opacity-0 scale-95 translate-y-2" @@click.away="open = false"
                                    class="absolute right-0 mt-3 w-80 dropdown-solid rounded-2xl shadow-2xl z-50">

                                    <!-- Enhanced User Header -->
                                    <div class="p-6 border-b border-slate-200">
                                        <div class="flex items-center space-x-4">
                                            <img src="@avatarUrl" alt="Avatar"
                                                class="w-14 h-14 rounded-xl border-3 border-gradient-to-r from-blue-500 to-purple-600 shadow-lg"
                                                data-user-avatar />
                                            <div class="flex-1">
                                                <p class="font-bold text-slate-800 text-base" data-user-fullname>@fullName
                                                </p>
                                                <p class="text-sm text-slate-600 mt-1" data-user-email>
                                                    @Context.Session.GetString("Email")</p>
                                                <span
                                                    class="inline-flex items-center px-3 py-1 bg-gradient-to-r from-blue-500 to-purple-600 text-white text-xs rounded-full font-semibold mt-2">
                                                    <i class="ri-vip-crown-line mr-1"></i>Premium
                                                </span>
                                            </div>
                                        </div>
                                    </div>

                                    <!-- Enhanced Menu Items -->
                                    <div class="p-3">
                                        <a asp-controller="Common" asp-action="Profile"
                                            class="flex items-center px-4 py-3 text-slate-700 hover:bg-blue-50 hover:text-blue-600 rounded-xl transition-all duration-200 group">
                                            <i
                                                class="ri-user-3-line text-lg mr-4 group-hover:scale-110 transition-transform"></i>
                                            <span class="font-semibold">My Profile</span>
                                        </a>
                                        <a asp-controller="Booking" asp-action="BookingSchedule"
                                            class="flex items-center px-4 py-3 text-slate-700 hover:bg-green-50 hover:text-green-600 rounded-xl transition-all duration-200 group">
                                            <i
                                                class="ri-calendar-check-line text-lg mr-4 group-hover:scale-110 transition-transform"></i>
                                            <span class="font-semibold">My Bookings</span>
                                        </a>
                                        <a href="#"
                                            class="flex items-center px-4 py-3 text-slate-700 hover:bg-purple-50 hover:text-purple-600 rounded-xl transition-all duration-200 group">
                                            <i
                                                class="ri-settings-3-line text-lg mr-4 group-hover:scale-110 transition-transform"></i>
                                            <span class="font-semibold">Settings</span>
                                        </a>
                                        <hr class="my-3 border-slate-200">
                                        <a asp-controller="Common" asp-action="Logout"
                                            class="flex items-center px-4 py-3 text-red-600 hover:bg-red-50 rounded-xl transition-all duration-200 group">
                                            <i
                                                class="ri-logout-circle-line text-lg mr-4 group-hover:scale-110 transition-transform"></i>
                                            <span class="font-semibold">Sign Out</span>
                                        </a>
                                    </div>
                                </div>
                            </div>
                        </div>
                    }
                    else
                    {
                        <!-- Enhanced Sign In Button -->
                        <a asp-controller="Common" asp-action="Login">
                            <button
                                class="btn-gradient text-white px-6 py-2.5 rounded-xl hover:shadow-lg transition-all duration-300 font-bold text-sm flex items-center space-x-2">
                                <i class="ri-login-circle-line text-base"></i>
                                <span>Sign In</span>
                            </button>
                        </a>
                    }

                    <!-- Mobile Menu Toggle -->
                    <button @@click="mobileMenuOpen = !mobileMenuOpen"
                        class="lg:hidden p-2.5 text-slate-600 hover:text-blue-600 transition-colors mobile-nav-toggle">
                        <i class="ri-menu-line text-xl" x-show="!mobileMenuOpen"></i>
                        <i class="ri-close-line text-xl" x-show="mobileMenuOpen"></i>
                    </button>
                </div>
            </div>
            <script>
                window.currentUserId = '@userId';
                
            </script>
            <!-- Mobile Navigation Menu -->
            <div x-show="mobileMenuOpen" x-transition:enter="transition ease-out duration-200"
                x-transition:enter-start="opacity-0 transform -translate-y-2"
                x-transition:enter-end="opacity-1 transform translate-y-0"
                x-transition:leave="transition ease-in duration-150"
                x-transition:leave-start="opacity-1 transform translate-y-0"
                x-transition:leave-end="opacity-0 transform -translate-y-2"
                class="lg:hidden border-t border-white/20 bg-white/95 backdrop-blur-lg">
                <div class="px-4 py-4 space-y-2">
                    <a href="#"
                        class="block px-4 py-3 text-slate-600 hover:text-blue-600 hover:bg-blue-50 rounded-xl transition-all">
                        <i class="ri-home-4-line mr-3"></i>Home
                    </a>

                    <a href="#"
                        class="block px-4 py-3 text-slate-600 hover:text-blue-600 hover:bg-blue-50 rounded-xl transition-all">
                        <i class="ri-team-line mr-3"></i>Find Teams
                    </a>

                    <a href="#"
                        class="block px-4 py-3 text-slate-600 hover:text-blue-600 hover:bg-blue-50 rounded-xl transition-all">
                        <i class="ri-information-line mr-3"></i>About
                    </a>

                    <!-- Mobile Search -->
                    <div class="pt-4 border-t border-slate-200">
                        <div class="relative">
                            <input type="text" placeholder="Search..."
                                class="w-full px-4 py-3 pl-11 bg-slate-100 border-0 rounded-xl focus:outline-none focus:ring-2 focus:ring-blue-500 text-slate-700">
                            <i
                                class="ri-search-2-line absolute left-3.5 top-1/2 transform -translate-y-1/2 text-slate-500"></i>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </header>

    <!-- Main content -->
    <main class="animate-fade-in">
        @RenderBody()
    </main>

    <!-- Enhanced Footer -->
    <footer class="bg-gradient-to-r from-slate-900 to-slate-800 text-white py-16 mt-16 relative overflow-hidden">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8 mb-12">
                <!-- Logo & social -->
                <div class="lg:col-span-1">
                    <div class="flex items-center space-x-3 mb-4">
                        <div
                            class="w-10 h-10 bg-gradient-to-r from-blue-500 to-purple-600 rounded-xl flex items-center justify-center">
                            <i class="ri-football-line text-white text-lg"></i>
                        </div>
                        <h3 class="font-poppins font-bold text-2xl logo-gradient">Sportivey</h3>
                    </div>
                    <p class="text-slate-300 mb-6 leading-relaxed">Your premier destination for booking sports stadiums
                        and connecting with teams.</p>
                    <div class="flex space-x-3">
                        <button
                            class="w-10 h-10 bg-slate-800 hover:bg-blue-600 rounded-lg transition-all duration-300 hover:scale-110 flex items-center justify-center">
                            <i class="ri-facebook-fill text-lg"></i>
                        </button>
                        <button
                            class="w-10 h-10 bg-slate-800 hover:bg-blue-600 rounded-lg transition-all duration-300 hover:scale-110 flex items-center justify-center">
                            <i class="ri-twitter-fill text-lg"></i>
                        </button>
                        <button
                            class="w-10 h-10 bg-slate-800 hover:bg-blue-600 rounded-lg transition-all duration-300 hover:scale-110 flex items-center justify-center">
                            <i class="ri-instagram-fill text-lg"></i>
                        </button>
                    </div>
                </div>

                <!-- Quick links -->
                <div>
                    <h4 class="text-lg font-semibold mb-4 text-white">Quick Links</h4>
                    <ul class="space-y-2">


                        <li><a href="#"
                                class="text-slate-300 hover:text-white transition-colors duration-200">Pricing</a></li>
                        <li><a href="#" class="text-slate-300 hover:text-white transition-colors duration-200">About
                                Us</a></li>
                    </ul>
                </div>

                <!-- Support -->
                <div>
                    <h4 class="text-lg font-semibold mb-4 text-white">Support</h4>
                    <ul class="space-y-2">
                        <li><a href="#" class="text-slate-300 hover:text-white transition-colors duration-200">Help
                                Center</a></li>
                        <li><a href="#" class="text-slate-300 hover:text-white transition-colors duration-200">Contact
                                Us</a></li>
                        <li><a href="#" class="text-slate-300 hover:text-white transition-colors duration-200">Terms of
                                Service</a></li>
                        <li><a href="#" class="text-slate-300 hover:text-white transition-colors duration-200">Privacy
                                Policy</a></li>
                    </ul>
                </div>

                <!-- Newsletter -->
                <div>
                    <h4 class="text-lg font-semibold mb-4 text-white">Stay Updated</h4>
                    <p class="text-slate-300 mb-4">Subscribe for updates and offers.</p>
                    <div class="space-y-3">
                        <input type="email" placeholder="Your email"
                            class="w-full px-4 py-2 bg-slate-800 border border-slate-700 rounded-lg text-white placeholder-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500">
                        <button class="w-full btn-gradient text-white px-4 py-2 rounded-lg font-semibold">
                            Subscribe
                        </button>
                    </div>
                </div>
            </div>

            <div class="border-t border-slate-700 pt-6 text-center">
                <p class="text-slate-300">&copy; 2025 Sportivey Platform. All rights reserved.</p>
            </div>
        </div>
    </footer>

    <!-- Scripts -->
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>

    <script>
        // Enhanced scroll behavior with performance optimization
        let lastScrollTop = 0;
        const header = document.querySelector('header');
        let ticking = false;

        function updateHeader() {
            let scrollTop = window.pageYOffset || document.documentElement.scrollTop;

            if (scrollTop > 100) {
                if (scrollTop > lastScrollTop && !header.classList.contains('header-hidden')) {
                    // Scrolling down
                    header.classList.add('header-hidden');
                    header.style.transform = 'translateY(-100%)';
                } else if (scrollTop < lastScrollTop && header.classList.contains('header-hidden')) {
                    // Scrolling up
                    header.classList.remove('header-hidden');
                    header.style.transform = 'translateY(0)';
                }
            } else {
                header.classList.remove('header-hidden');
                header.style.transform = 'translateY(0)';
            }

            lastScrollTop = scrollTop;
            ticking = false;
        }

        window.addEventListener('scroll', function () {
            if (!ticking) {
                requestAnimationFrame(updateHeader);
                ticking = true;
            }
        }, { passive: true });

        // Enhanced keyboard navigation
        document.addEventListener('keydown', function (e) {
            // Escape key closes all dropdowns
            if (e.key === 'Escape') {
                // Dispatch Alpine.js event to close all dropdowns
                document.dispatchEvent(new CustomEvent('keydown-escape'));
            }

            // Ctrl/Cmd + K opens search
            if ((e.ctrlKey || e.metaKey) && e.key === 'k') {
                e.preventDefault();
                // Trigger search expansion
                const searchButton = document.querySelector('[x-on\\:click="toggleSearch()"]');
                if (searchButton) {
                    searchButton.click();
                }
            }
        });

        // Enhanced button interactions with better performance
        const observerOptions = {
            threshold: 0.1,
            rootMargin: '50px'
        };

        const buttonObserver = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    const btn = entry.target;
                    btn.addEventListener('mouseenter', handleButtonHover);
                    btn.addEventListener('mouseleave', handleButtonLeave);
                } else {
                    const btn = entry.target;
                    btn.removeEventListener('mouseenter', handleButtonHover);
                    btn.removeEventListener('mouseleave', handleButtonLeave);
                }
            });
        }, observerOptions);

        function handleButtonHover(e) {
            this.style.transform = 'translateY(-2px) scale(1.05)';
        }

        function handleButtonLeave(e) {
            this.style.transform = 'translateY(0) scale(1)';
        }

        // Observe all action buttons
        document.querySelectorAll('.action-btn').forEach(btn => {
            buttonObserver.observe(btn);
        });

        // Performance optimization
        window.addEventListener('load', function () {
            document.body.classList.add('loaded');

            // Preload critical resources
            const criticalImages = [
                '/favicon.png'
            ];

            criticalImages.forEach(src => {
                const img = new Image();
                img.src = src;
            });
        });

        // Enhanced tooltip system
        function initTooltips() {
            const tooltipElements = document.querySelectorAll('[title]');

            tooltipElements.forEach(element => {
                element.addEventListener('mouseenter', showTooltip);
                element.addEventListener('mouseleave', hideTooltip);
            });
        }

        function showTooltip(e) {
            const title = this.getAttribute('title');
            if (!title) return;

            this.setAttribute('data-title', title);
            this.removeAttribute('title');

            const tooltip = document.createElement('div');
            tooltip.className = 'tooltip show';
            tooltip.textContent = title;

            document.body.appendChild(tooltip);

            const rect = this.getBoundingClientRect();
            tooltip.style.left = rect.left + (rect.width / 2) - (tooltip.offsetWidth / 2) + 'px';
            tooltip.style.top = rect.top - tooltip.offsetHeight - 8 + 'px';

            this._tooltip = tooltip;
        }

        function hideTooltip(e) {
            const title = this.getAttribute('data-title');
            if (title) {
                this.setAttribute('title', title);
                this.removeAttribute('data-title');
            }

            if (this._tooltip) {
                this._tooltip.remove();
                delete this._tooltip;
            }
        }

        // Initialize tooltips when DOM is ready
        document.addEventListener('DOMContentLoaded', initTooltips);
    </script>
    <script src="https://translate.google.com/translate_a/element.js?cb=initGoogleTranslate"></script>
    <script>
        function initGoogleTranslate() {
          new google.translate.TranslateElement({
            pageLanguage: 'en',
            includedLanguages: 'en,vi,ja,ko,zh-CN',
            autoDisplay: false,
            multilanguagePage: true
          }, 'google_translate_element');

          // Ẩn banner Google
          const handleGoogleUI = () => {
            document.querySelectorAll('.goog-te-banner-frame, .goog-te-banner').forEach(b => b.remove());
            document.body.style.top = '0px';
          };

          const observer = new MutationObserver(() => handleGoogleUI());
          observer.observe(document.body, { childList: true, subtree: true });
        }

        // Toggle menu custom
        const toggleBtn = document.getElementById("translate-toggle");
        const menu = document.getElementById("translate-menu");

        toggleBtn.addEventListener("click", () => {
          menu.classList.toggle("hidden");
        });

        document.addEventListener("click", (e) => {
          if (!toggleBtn.contains(e.target) && !menu.contains(e.target)) {
            menu.classList.add("hidden");
          }
        });

    
        document.querySelectorAll("#translate-menu [data-lang]").forEach(btn => {
          btn.addEventListener("click", () => {
            const lang = btn.getAttribute("data-lang");
            const select = document.querySelector("#google_translate_element select");
            if (select) {
              select.value = lang;
              select.dispatchEvent(new Event("change"));
            }
            menu.classList.add("hidden");

           
            setTimeout(() => {
              document.querySelectorAll("[data-translate-placeholder]").forEach(el => {
                el.setAttribute("placeholder", el.getAttribute("data-translate-" + lang) || el.placeholder);
              });
            }, 500);
          });
        });


        document.getElementById("reset-translate-btn").addEventListener("click", function() {
        document.cookie = 'googtrans=;expires=Thu, 01 Jan 1970 00:00:01 GMT;path=/;';
          if (window.localStorage) {
            localStorage.removeItem('googtrans');
          }
         window.location.reload();
        });
    </script>
    @RenderSection("Scripts", required: false)



    <script>
        // Simplified function - sử dụng session data từ server
        window.refreshHeaderFromSession = function () {
            console.log('🔄 Refreshing header with latest session data from server...');

            fetch('/Common/GetUserHeaderInfo')
                .then(response => response.json())
                .then(data => {
                    console.log('✅ Latest session data:', data);

                    // Update avatar images
                    document.querySelectorAll('img[data-user-avatar]').forEach(img => {
                        const newSrc = `${data.avatarUrl}?t=${Date.now()}`;
                        console.log(`Avatar: ${img.src} → ${newSrc}`);
                        img.src = newSrc;
                    });

                    // Update names
                    document.querySelectorAll('[data-user-firstname]').forEach(el => {
                        el.textContent = data.fullName.split(' ')[0] || 'User';
                    });

                    document.querySelectorAll('[data-user-fullname]').forEach(el => {
                        el.textContent = data.fullName;
                    });

                    document.querySelectorAll('[data-user-email]').forEach(el => {
                        el.textContent = data.email;
                    });

                    console.log('✅ Header updated successfully');
                })
                .catch(error => {
                    console.error('❌ Failed to refresh header from session:', error);
                });
        };
    </script>

    <script>
        (function () {
            function deleteCookie(name) {
                try {
                    document.cookie = name + "=; expires=Thu, 01 Jan 1970 00:00:00 GMT; path=/;";
                    document.cookie = name + "=; expires=Thu, 01 Jan 1970 00:00:00 GMT; path=/; domain=" + location.hostname + ";";
                    if (location.hostname.indexOf('.') !== -1) {
                        document.cookie = name + "=; expires=Thu, 01 Jan 1970 00:00:00 GMT; path=/; domain=." + location.hostname + ";";
                    }
                } catch (e) { console.warn('Cookie delete failed', e); }
            }

            function stopTranslateAndReload() {
                // Xóa cookie Google Translate
                deleteCookie('googtrans');

                // Thêm nếu bạn gặp cookie khác liên quan: (ví dụ) deleteCookie('googtrans'); deleteCookie('__gtrans');
                // Sau đó reload trang để trạng thái dịch bị loại bỏ
                location.reload();
            }

            document.addEventListener('DOMContentLoaded', function () {
                const btn = document.getElementById('reset-translate-btn');
                if (!btn) return;
                btn.addEventListener('click', function () {
                    btn.disabled = true;
                    btn.textContent = 'Đang đặt...';
                    setTimeout(() => {
                        stopTranslateAndReload();
                    }, 150);
                });
            });
        })();

    </script>
    <script>
        function connectToSignalR() {
            const connection = new signalR.HubConnectionBuilder()
                .withUrl("https://localhost:7072/notificationHub")
                .build();

            // Lắng nghe sự kiện "ReceiveNotification"
            connection.on("ReceiveNotification", function (notification) {
                console.log("Nhận thông báo mới:", notification);

                // Nếu không có parameters, delay 3 giây rồi cập nhật badge
                setTimeout(function () {
                    updateNotificationBadge();
                }, 3000);

                 if (typeof onReceiveStadiumNotification === "function") {
        onReceiveStadiumNotification(notification);
    }
                // Hiển thị thông báo toast nếu muốn
                // showToast(notification.title, notification.message);
            });

            // Kết nối tới hub
            connection.start()
                .then(function () {
                    console.log("Đã kết nối tới notification hub!");
                })
                .catch(function (err) {
                    console.error("Kết nối SignalR thất bại:", err);
                });
        }

        async function updateNotificationBadge() {
            try {
                const resp = await fetch('/Notification/GetUnreadNotificationCount');
                if (resp.ok) {
                    const count = await resp.json();
                    const badge = document.getElementById('notification-badge');
                    console.log('Unread notifications:', count);
                    if (badge) {
                        if (count > 0) {
                            badge.textContent = count;
                            badge.style.display = 'flex';
                        } else {
                            badge.style.display = 'none';
                        }
                    }
                }
            } catch (err) {
                console.error('Error fetching notification count:', err);
            }
        }

        // Gọi khi DOM đã sẵn sàng
        $(document).ready(function () {
            connectToSignalR();
            updateNotificationBadge();
        });
    </script>
    <script>
        window.currentUserId = @userId;
        console.log("🔑 UserId từ Session:", @userId);
    </script>
    <script type="module">
        import { initializeApp } from "https://www.gstatic.com/firebasejs/10.13.1/firebase-app.js";
        import { getDatabase, ref, get, set, onValue, onChildAdded } from "https://www.gstatic.com/firebasejs/10.13.1/firebase-database.js";

        const firebaseConfig = {
            apiKey: "AIzaSyCg6rxISbUgZKNqqLlkBrYS_sQtkYRaByo",
            authDomain: "chatbox-993b2.firebaseapp.com",
            databaseURL: "https://chatbox-993b2-default-rtdb.firebaseio.com",
            projectId: "chatbox-993b2",
            storageBucket: "chatbox-993b2.appspot.com",
            messagingSenderId: "168483987205",
            appId: "1:168483987205:web:c9ef776901ec2882bb616c"
        };

        const app = initializeApp(firebaseConfig);
        const db = getDatabase(app);

        if (Notification.permission !== "granted") {
            Notification.requestPermission();
        }

        async function getUserRooms(userId) {
            const snapshot = await get(ref(db, "chats"));
            const rooms = [];

            if (snapshot.exists()) {
                snapshot.forEach(roomSnap => {
                    const roomId = roomSnap.key;
                    if (roomId.split("_").includes(String(userId))) {
                        rooms.push(roomId);
                    }
                });
            }

            return rooms;
        }

        function showNotification(msg) {
            if (Notification.permission === "granted") {
                new Notification(`${msg.senderName}`, { body: msg.message });
            }
        }

        // ✅ Tăng số tin chưa đọc trong Firebase
               import { runTransaction } from "https://www.gstatic.com/firebasejs/10.13.1/firebase-database.js";
        async function incrementUnread(userId) {
            const unreadRef = ref(db, `unreadMessages/${userId}`);
            await runTransaction(unreadRef, (current) => (current || 0) + 1);
        }

        async function listenForUserMessages(userId, badgeHandler) {
            const rooms = await getUserRooms(userId);

            // ✅ Lắng nghe thay đổi số tin chưa đọc từ Firebase
            const unreadRef = ref(db, `unreadMessages/${userId}`);
            onValue(unreadRef, (snapshot) => {
                const count = snapshot.exists() ? snapshot.val() : 0;
                badgeHandler(count);
            });

            // ✅ Lắng nghe tin mới
            const startTime = Date.now();

            rooms.forEach(roomId => {
                const roomRef = ref(db, `chats/${roomId}`);

                onChildAdded(roomRef, (snapshot) => {
                    const msg = snapshot.val();

                    if (msg.timestamp > startTime && msg.senderId != userId) {
                       
                        showNotification(msg);
                    }
                });
            });
        }

        const currentUserId = window.currentUserId;

        if (currentUserId) {
            window.listenForUserMessages = (badgeHandler) => {
                return listenForUserMessages(currentUserId, badgeHandler);
            };

            // ✅ Reset số tin chưa đọc về 0 khi mở chat
            window.resetUnreadMessages = async () => {
                const unreadRef = ref(db, `unreadMessages/${currentUserId}`);
                await set(unreadRef, 0);
            };
        }
    </script>

    <script>
        function chatBadge() {
            return {
                unreadCount: 0,
                async openChat() {
                    // ✅ Reset badge về 0 trong Firebase
                    if (typeof window.resetUnreadMessages === "function") {
                        await window.resetUnreadMessages();
                    }
                    this.unreadCount = 0;

                    // Chuyển sang trang chat
                    window.location.href = '@Url.Action("Chat", "Chat")';
                },
                setBadge(count) {
                    this.unreadCount = count;
                },
                init() {
                    setTimeout(() => {
                        if (typeof window.listenForUserMessages === "function") {
                            window.listenForUserMessages((count) => {
                                this.setBadge(count);
                            });
                        }
                    }, 500);
                }
            }
        }
    </script>
</body>

</html>